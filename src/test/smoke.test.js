import { describe, it, expect } from 'vitest';

describe('Initial Test Suite', () => {
  it('should perform basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });

  it('should verify the environment is working', () => {
    expect(true).toBe(true);
  });
});
